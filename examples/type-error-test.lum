// ============================================================
// Lumina Example: Type Error Test
// This example intentionally has type errors
// ============================================================

component TypeErrorDemo() {
  // Type error: assigning string to Int
  state count: Int = "not a number"

  // Type error: assigning number to String
  state message: String = 42

  fn badFunction(x: Int) {
    // Type error: adding Int to String
    let result: Int = x + "hello"
    return result
  }

  <div>
    <p>{count}</p>
  </div>
}
